@extends('layouts.app')

@section('content')
<div class="min-h-screen bg-gradient-to-br from-green-50 to-emerald-100">
    <div class="container mx-auto px-4 py-6 max-w-6xl">
        
        <!-- Mobile Header -->
        <div class="mb-6 animate-fade-in">
            <div class="flex flex-col md:flex-row md:justify-between md:items-center space-y-4 md:space-y-0">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-900 mb-2">
                        {{ $isFrench ? 'Résultat de l\'analyse' : 'Analysis result' }}
                    </h1>
                    <p class="text-gray-600 text-sm md:text-base">
                        {{ $isFrench ? 'Réponse générée par Sherlock Recette' : 'Response generated by Sherlock Recipe' }}
                    </p>
                </div>
                
                <div class="animate-fade-in delay-100">
                    @include('buttons')
                </div>
            </div>
        </div>

        <!-- Question Card -->
        <div class="bg-white rounded-2xl shadow-xl overflow-hidden mb-6 animate-scale-in">
            <div class="bg-gradient-to-r from-blue-600 to-indigo-700 px-6 py-4">
                <h3 class="text-lg font-medium text-white flex items-center">
                    <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.228 9c.549-1.165 2.03-2 3.772-2 2.21 0 4 1.343 4 3 0 1.4-1.278 2.575-3.006 2.907-.542.104-.994.54-.994 1.093m0 3h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                    {{ $isFrench ? 'Votre question' : 'Your question' }}
                </h3>
            </div>
            <div class="p-6">
                <div class="bg-gray-50 rounded-lg p-4 border border-gray-200">
                    <p class="text-gray-700 leading-relaxed">{{ $query }}</p>
                </div>
            </div>
        </div>

        <!-- Response Card -->
        <div class="bg-white rounded-2xl shadow-xl overflow-hidden animate-fade-in-up" style="animation-delay: 0.2s">
            <div class="bg-gradient-to-r from-green-600 to-emerald-700 px-6 py-4">
                <h3 class="text-xl font-medium text-white flex items-center">
                    <svg class="w-6 h-6 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path>
                    </svg>
                    {{ $isFrench ? 'Réponse de Sherlock Recette' : 'Sherlock Recipe Response' }}
                </h3>
            </div>
            
            <div class="p-6">
                <div class="prose prose-lg max-w-none bg-white rounded-lg border border-gray-200 shadow-sm p-6">
                    <div class="response-content text-gray-800 leading-relaxed markdown-content">
                        {{-- Affichage du contenu Markdown parsé --}}
                        {!! \Illuminate\Support\Str::markdown($response) !!}
                    </div>
                </div>
            </div>
        </div>

        <!-- Action Buttons -->
        <div class="mt-8 flex flex-col md:flex-row justify-between space-y-3 md:space-y-0 md:space-x-3 animate-fade-in-up" style="animation-delay: 0.3s">
            <a href="{{ route('sherlock.recipes.index') }}" 
               class="inline-flex items-center justify-center px-6 py-3 border border-gray-300 shadow-sm text-sm font-medium rounded-lg text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition-all duration-200">
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.228 9c.549-1.165 2.03-2 3.772-2 2.21 0 4 1.343 4 3 0 1.4-1.278 2.575-3.006 2.907-.542.104-.994.54-.994 1.093m0 3h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
                {{ $isFrench ? 'Poser une autre question' : 'Ask another question' }}
            </a>
            
            @if (auth()->check() && !$isRecipeGeneration)
            <button type="button" 
                    onclick="window.print()" 
                    class="inline-flex items-center justify-center px-6 py-3 border border-transparent text-sm font-medium rounded-lg shadow-sm text-white bg-gradient-to-r from-indigo-600 to-blue-600 hover:from-indigo-700 hover:to-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition-all duration-200 transform hover:scale-105">
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 17h2a2 2 0 002-2v-4a2 2 0 00-2-2H5a2 2 0 00-2 2v4a2 2 0 002 2h2m2 4h6a2 2 0 002-2v-4a2 2 0 00-2-2H9a2 2 0 00-2 2v4a2 2 0 002 2zm8-12V5a2 2 0 00-2-2H9a2 2 0 00-2 2v4h10z"></path>
                </svg>
                {{ $isFrench ? 'Imprimer' : 'Print' }}
            </button>
            @endif
        </div>
    </div>
</div>

<!-- Mobile-First CSS Styles -->
<style>
/* Print Styles */
@media print {
    nav, header, footer, .hidden-print, button, a[href] {
        display: none !important;
    }
    
    body {
        background: white !important;
        font-size: 12pt;
        color: black !important;
    }
    
    .response-content {
        page-break-inside: auto;
    }
    
    h1, h2, h3 {
        page-break-after: avoid;
        color: black !important;
    }
    
    .bg-white {
        background: white !important;
        box-shadow: none !important;
    }
}

/* Animations */
@keyframes fade-in {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
}

@keyframes fade-in-up {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
}

@keyframes scale-in {
    from { opacity: 0; transform: scale(0.95); }
    to { opacity: 1; transform: scale(1); }
}

.animate-fade-in {
    animation: fade-in 0.6s ease-out;
}

.animate-fade-in-up {
    animation: fade-in-up 0.8s ease-out;
    opacity: 0;
    animation-fill-mode: forwards;
}

.animate-scale-in {
    animation: scale-in 0.5s ease-out;
}

.delay-100 { animation-delay: 0.1s; }

/* Styles spécifiques pour le contenu Markdown */
.markdown-content {
    line-height: 1.7;
    color: #374151;
}

.markdown-content h1 {
    font-size: 2rem;
    font-weight: 700;
    color: #059669;
    margin-top: 2rem;
    margin-bottom: 1rem;
    border-bottom: 2px solid #d1fae5;
    padding-bottom: 0.5rem;
}

.markdown-content h2 {
    font-size: 1.75rem;
    font-weight: 600;
    color: #047857;
    margin-top: 1.75rem;
    margin-bottom: 0.75rem;
}

.markdown-content h3 {
    font-size: 1.5rem;
    font-weight: 600;
    color: #065f46;
    margin-top: 1.5rem;
    margin-bottom: 0.75rem;
}

.markdown-content h4 {
    font-size: 1.25rem;
    font-weight: 600;
    color: #064e3b;
    margin-top: 1.25rem;
    margin-bottom: 0.5rem;
}

.markdown-content p {
    margin-bottom: 1.25rem;
    text-align: justify;
}

.markdown-content ul {
    list-style-type: disc;
    margin-left: 1.5rem;
    margin-bottom: 1.25rem;
    padding-left: 0;
}

.markdown-content ol {
    list-style-type: decimal;
    margin-left: 1.5rem;
    margin-bottom: 1.25rem;
    padding-left: 0;
}

.markdown-content li {
    margin-bottom: 0.5rem;
    line-height: 1.6;
}

.markdown-content li p {
    margin-bottom: 0.5rem;
}

.markdown-content strong {
    font-weight: 700;
    color: #1f2937;
}

.markdown-content em {
    font-style: italic;
    color: #4b5563;
}

.markdown-content code {
    background-color: #f3f4f6;
    color: #d97706;
    padding: 0.125rem 0.25rem;
    border-radius: 0.25rem;
    font-size: 0.875em;
    font-family: 'Courier New', monospace;
}

.markdown-content pre {
    background-color: #1f2937;
    color: #f9fafb;
    padding: 1rem;
    border-radius: 0.5rem;
    overflow-x: auto;
    margin: 1rem 0;
}

.markdown-content pre code {
    background-color: transparent;
    color: inherit;
    padding: 0;
}

.markdown-content blockquote {
    border-left: 4px solid #10b981;
    padding-left: 1rem;
    margin: 1.5rem 0;
    background-color: #f0fdf4;
    padding: 1rem;
    border-radius: 0.25rem;
    font-style: italic;
}

.markdown-content table {
    width: 100%;
    border-collapse: collapse;
    margin: 1.5rem 0;
}

.markdown-content th,
.markdown-content td {
    border: 1px solid #d1d5db;
    padding: 0.75rem;
    text-align: left;
}

.markdown-content th {
    background-color: #f9fafb;
    font-weight: 600;
    color: #374151;
}

.markdown-content tr:nth-child(even) {
    background-color: #f9fafb;
}

/* Mobile Responsive pour le contenu Markdown */
@media (max-width: 768px) {
    .container {
        padding-left: 1rem;
        padding-right: 1rem;
    }
    
    .markdown-content {
        font-size: 0.95rem;
    }
    
    .markdown-content h1 {
        font-size: 1.75rem;
    }
    
    .markdown-content h2 {
        font-size: 1.5rem;
    }
    
    .markdown-content h3 {
        font-size: 1.25rem;
    }
    
    .markdown-content h4 {
        font-size: 1.125rem;
    }
    
    .markdown-content ul,
    .markdown-content ol {
        margin-left: 1rem;
    }
    
    .markdown-content table {
        font-size: 0.875rem;
    }
    
    .markdown-content pre {
        font-size: 0.8rem;
        padding: 0.75rem;
    }
    
    /* Touch-friendly buttons */
    button, a {
        min-height: 44px;
        touch-action: manipulation;
    }
    
    /* Mobile spacing */
    .md\:flex-row {
        flex-direction: column;
    }
    
    .md\:space-x-3 > * + * {
        margin-left: 0;
        margin-top: 0.75rem;
    }
}

/* Enhanced hover effects */
.hover\:scale-105:hover {
    transform: scale(1.05);
}

/* Amélioration de la lisibilité */
.markdown-content {
    word-wrap: break-word;
    overflow-wrap: break-word;
}

/* Style pour les listes imbriquées */
.markdown-content ul ul,
.markdown-content ol ol,
.markdown-content ul ol,
.markdown-content ol ul {
    margin-top: 0.5rem;
    margin-bottom: 0.5rem;
}
</style>
@endsection